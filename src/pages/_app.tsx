import "../styles/global.css";
// countdown depende de challengesprovider por isso está por dentro

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
